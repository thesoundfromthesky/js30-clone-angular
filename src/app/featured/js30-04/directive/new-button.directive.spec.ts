import { NewButtonDirective } from './new-button.directive';

describe('NewButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NewButtonDirective();
    expect(directive).toBeTruthy();
  });
});
