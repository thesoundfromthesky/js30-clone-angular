import { SortButtonDirective } from './sort-button.directive';

describe('SortButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SortButtonDirective();
    expect(directive).toBeTruthy();
  });
});
