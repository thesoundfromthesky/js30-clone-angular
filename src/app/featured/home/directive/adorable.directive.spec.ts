import { AdorableDirective } from './adorable.directive';

describe('AdorableDirective', () => {
  it('should create an instance', () => {
    // const directive = new AdorableDirective();
    // expect(directive).toBeTruthy();
  });
});
